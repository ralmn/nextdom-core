version: "3.5"
services:
  nextdom-test:
    image: nextdom/nextdom-core:test
    container_name: nextdom-test
    networks:
      - default
    ports:
      - 8765:80
    volumes:
      - type: bind
        source: ../../../
        target: /data
    build:
      context: ../../../
      dockerfile: docker/build/test/Dockerfile
      labels:
        - org.label-schema.schema-version="1.0"
        - org.label-schema.build-date="$BUILD_DATE"
        - org.label-schema.name="nextdom/nextdom-core:test"
        - org.label-schema.description="Use to run nextdom core functionnal test suite"
        - org.label-schema.url="https://www.nextdom.org/"
        - org.label-schema.vcs-url="https://github.com/NextDom/nextdom-core/"
        - org.label-schema.vcs-ref="$VCS_REF"
        - org.label-schema.vendor="nextdom"
        - org.label-schema.version="$BUILD_VERSION"
networks:
  default:
    name: nextdom-test
